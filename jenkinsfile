pipeline {
    agent none
    stages {
        stage("Build & SonarQube Analysis") {
            agent any
            steps {
                withSonarQubeEnv('Sonarqubeserver') {
                    sh 'mvn clean package sonar:sonar'
                }
            }
        }
    }
}
